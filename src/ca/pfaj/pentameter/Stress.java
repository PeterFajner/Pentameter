package ca.pfaj.pentameter;

/**
 * Stress is the stress on a single syllable. It can be HIGH (stressed),
 * LOW (unstressed), or SILENT (not pronounced, like a " ").
 */
public enum Stress {
    HIGH,
    LOW,
    SILENT,
}
